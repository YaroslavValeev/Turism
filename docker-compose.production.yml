services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    env_file:
      - .env.production
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backups:/backups
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 10

  api:
    build:
      # host-сеть: на части VPS маршрут до npm/registry лучше, чем из bridge.
      context: .
      dockerfile: services/api/Dockerfile
      network: host
      args:
        NPM_CONFIG_REGISTRY: ${NPM_CONFIG_REGISTRY:-https://registry.npmjs.org/}
        PRISMA_ENGINES_MIRROR: ${PRISMA_ENGINES_MIRROR:-}
        RELEASE_SHA: ${EXPECTED_SHA:-unknown}
        RELEASE_BUILT_AT: ${RELEASE_BUILT_AT:-}
        GITHUB_RUN_ID: ${GITHUB_RUN_ID:-}
    restart: unless-stopped
    env_file:
      - .env.production
      - services/api/.env.production
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ingestion_media:/app/apps/web/public/ingestion-media
      - ./logs/api:/var/log/mywave/api
    expose:
      - "3001"
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "node -e \"require('http').get('http://127.0.0.1:3001/health',r=>{let d='';r.on('data',c=>d+=c);r.on('end',()=>process.exit(r.statusCode===200&&d.includes('ok')?0:1))}).on('error',()=>process.exit(1))\"",
        ]
      interval: 20s
      timeout: 5s
      retries: 5
      start_period: 40s

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
      network: host
      args:
        NPM_CONFIG_REGISTRY: ${NPM_CONFIG_REGISTRY:-https://registry.npmjs.org/}
    restart: unless-stopped
    env_file:
      - .env.production
      - apps/web/.env.production
    depends_on:
      - api
    # Тот же named volume, что у api — иначе URL /ingestion-media/* отдаёт web без файлов → 404 в витрине.
    volumes:
      - ingestion_media:/app/apps/web/public/ingestion-media
    expose:
      - "3000"

  admin:
    build:
      context: .
      dockerfile: apps/admin/Dockerfile
      network: host
      args:
        NPM_CONFIG_REGISTRY: ${NPM_CONFIG_REGISTRY:-https://registry.npmjs.org/}
        # Next.js embeds NEXT_PUBLIC_* values into the browser bundle at build time.
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-https://api.mywavetour.ru}
    restart: unless-stopped
    env_file:
      - .env.production
      - apps/admin/.env.production
    depends_on:
      - api
    expose:
      - "3002"

  reverse-proxy:
    image: nginx:1.27-alpine
    restart: unless-stopped
    # web/api/admin в default compose-сети (без network:host), иначе nginx не достучится до web:3000 → 502.
    depends_on:
      - web
      - admin
      - api
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./infra/nginx/mywave.conf:/etc/nginx/conf.d/default.conf:ro
      - ./infra/nginx/certs:/etc/nginx/certs:ro
      - ./infra/certbot-webroot:/var/www/certbot:ro
      - ./logs/nginx:/var/log/nginx

volumes:
  postgres_data:
  ingestion_media:
