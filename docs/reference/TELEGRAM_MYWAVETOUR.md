# Telegram MyWaveTour — канонические идентификаторы

Операционная справка: куда подставлять ID в переменных окружения API (`services/api/.env`, прод-секреты).

| Назначение | Chat / ID | Описание |
|------------|-----------|----------|
| Канал (публикации обновлений / витрина в TG) | **`-1003491522243`** | **MyWaveTour** — агрегатор экстремальных лагерей, camp, туров России |
| Бот | **`8345414467`** | **MyWaveTour** бот (сервисный аккаунт; токен бота хранится только в секретах, не в этом файле) |

## Соответствие env (типично)

- Публикация новых программ в канал обновлений: **`TELEGRAM_UPDATES_CHANNEL_CHAT_ID=-1003491522243`** (или тот же ID в формате, который ожидает Bot API для вашего канала).
- Алерты / owner: **`TELEGRAM_ALERT_CHAT_ID`** / **`TELEGRAM_CONTENT_OWNER_CHAT_ID`** — по политике деплоя (не обязательно совпадают с каналом выше).

## Важно

- Числовые ID каналов в Telegram часто начинаются с **`-100`** для супергрупп/каналов.
- Не коммитьте **токен** бота; в git допустимы только эти публично-операционные **chat_id**, если политика репозитория позволяет.
